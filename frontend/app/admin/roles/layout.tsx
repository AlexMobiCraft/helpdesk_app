'use client';

import React from 'react';

/**
 * Layout для маршрутов /admin/roles и вложенных.
 */
export default function RolesLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
